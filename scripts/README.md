# Scripts

Operational scripts (deploy helpers, backup/restore, release tagging) land
here as later phases need them. The one script that exists today - the
database seed - lives in [`database/seed/`](../database/seed/) instead,
since it's data setup rather than an ops task.
