# Docker

Empty until Phase 6 (Docker). It will hold the application `Dockerfile`
(multi-stage build), `docker-compose.yml` for local Postgres/Redis, and
`.dockerignore`. See [`docs/devops-roadmap.md`](../docs/devops-roadmap.md).
