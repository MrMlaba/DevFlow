# Terraform

Empty until Phase 12 (Terraform). It will hold `main.tf`, `variables.tf`,
`outputs.tf`, `providers.tf`, `vpc.tf`, `iam.tf`, `ecr.tf`, and `rds.tf`,
replacing infrastructure that Phase 11 provisions manually in the AWS
console. See [`docs/devops-roadmap.md`](../docs/devops-roadmap.md).
