# Tests

Empty until Phase 5 (Testing). It will hold:

- `tests/unit/` - Jest + React Testing Library unit tests
- `tests/e2e/` - Playwright end-to-end tests

See [`docs/devops-roadmap.md`](../docs/devops-roadmap.md) for the plan and
[`docs/development.md`](../docs/development.md) for how tests will be run
locally and in CI.
