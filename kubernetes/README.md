# Kubernetes manifests

Empty until Phase 13 (Kubernetes locally, with Kind/Minikube). It will hold
`namespace.yaml` and per-service manifests under `frontend/`, `backend/`,
`worker/`, `redis/`, and `ingress/`. Phase 14 adds AWS EKS-specific
configuration on top of these. See
[`docs/devops-roadmap.md`](../docs/devops-roadmap.md).
